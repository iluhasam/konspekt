---
tags:
  - конспект
  - devlog
type: devlog
created: 2026-07-22
---

# Dev Log — Конспект

Хронология работы над проектом. Новые записи сверху.

## 2026-07-22 — MVP завершён (Task 9: бот-слой и точка входа)

- Реализовал последний этап по плану: `src/bot/keyboard.ts` (`triageKeyboard`,
  `parseCallback` — callback-data `detail|done|delete:<id>`, TDD: тест сначала
  падал на отсутствующем модуле, затем 3 теста зелёные), `src/bot/bot.ts`
  (grammY-хендлеры: текст → URL, документ → файл, `callback_query` → триаж;
  тонкий I/O-слой без юнит-тестов по плану) и `src/index.ts` (грузит `.env`
  через `dotenv`, конфиг, Prisma, собирает бота, запускает long-polling).
- Добавил зависимость `dotenv`.
- Прогнал весь набор тестов — 13 файлов / 55 тестов зелёные
  (`DATABASE_URL="file:./dev.db" npm test`), `npx tsc --noEmit` — без ошибок.
- Доп. smoke-check импорта `pdf-parse` через барель экстракторов —
  `IMPORT_OK`, падений при импорте не обнаружено.
- **Ручная проверка в Telegram (Step 10) с реальными ключами не выполнялась** —
  ключей `TELEGRAM_BOT_TOKEN` / `OPENROUTER_API_KEY` в этой сессии нет; это
  единственный оставшийся ручной шаг для владельца проекта.
- Тем самым все 9 задач плана реализации MVP закрыты.

## 2026-07-22 — Старт проекта

- Провёл брейншторм: цель — реально пользоваться самому, боль — «цифровой хлам»
  (сохранёнки, которые не открываются). Механика — AI-конспект + триаж в Telegram.
- Финализировал дизайн MVP: бот принимает статью / YouTube / документ → конспект
  (суть + тезисы + теги) сообщениями в чат, кнопки `Подробнее / Готово / Удалить`.
- Определился со стеком — см. [[ADR-0001 — стек и архитектура]].
- Записал спеку: `docs/superpowers/specs/2026-07-22-konspekt-design.md`.
- Настроил структуру репо, Obsidian-документацию, публичный репозиторий
  `iluhasam/konspekt`.
- **Дальше:** план реализации (writing-plans) → каркас проекта и код по этапам.

## Связанные заметки

- [[Конспект — контекст]]
- [[ADR-0001 — стек и архитектура]]
